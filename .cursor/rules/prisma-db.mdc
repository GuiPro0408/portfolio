---
alwaysApply: false
description: Prisma, Postgres, migrations, and seeding
---
## Client usage

- Import the singleton from [src/lib/db.ts](mdc:src/lib/db.ts). It sets a global in development to avoid multiple instances.

```ts
import { prisma } from "@/lib/db";
const projects = await prisma.project.findMany();
```

## Schema and migrations

- Edit the schema in [prisma/schema.prisma](mdc:prisma/schema.prisma).
- After changes (including NextAuth tables), locally run:

```bash
npx prisma generate
npx prisma migrate dev --name <change>
```

- In production deployments (e.g., Vercel), use:

```bash
npm run migrate:deploy
```

## Seeding

- Seed with [scripts/seed.ts](mdc:scripts/seed.ts):

```bash
npm run seed
```

- Do not seed in production.

## Authentication

- NextAuth config lives in [src/lib/auth.ts](mdc:src/lib/auth.ts) with Prisma Adapter and Credentials provider (placeholder).
- API route at [src/app/api/auth/[...nextauth]/route.ts](mdc:src/app/api/auth/[...nextauth]/route.ts).

## Environment and local DB

- Requires `DATABASE_URL`. Example (from docs):

```env
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/portfolio?schema=public"
```

- Start local Postgres via [docker-compose.yml](mdc:docker-compose.yml):

```bash
docker compose up -d
```

